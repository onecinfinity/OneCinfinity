# Dynamic single_services page - kept locally only
single_services/

# Claude launch config
.claude/launch.json

# Schema reference files (embedded directly in HTML)
local Schema.txt
organization schema.txt
website Schema.txt
